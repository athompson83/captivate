# Captivate foundation adoption — 2026-09-10

Reviewed base: `215bd033ccd9a519704e246cad2b51c8a2104f45` (`main`). Read existing AGENTS.md, CLAUDE.md, and branch metadata. CLAUDE.md already imports AGENTS.md and is unchanged.
The prior technical manual is preserved byte-for-byte as blob `fbd85f09d084faba23569d5ec533f437d6d9f94c` in `docs/agents/operating-reference-2026-09-10.md`; the generated Next.js guidance remains at the root.

The concise entry point preserves Captivate's visual/camera, autosave, audience-data, renderer, schema, RLS, and existing verification rules while distinguishing implementation from real runtime/visual evidence.
Application code, canonical control standard, existing checklist/progress statuses, workflows, dependencies, migrations, secrets, and platform settings are unchanged.

NOT_RUN here: clean-checkout install/start/smoke, npm verification, shader/lifecycle/E2E/RLS suites, deliberate-failure test, fresh-agent instruction loading, CI routing checks, physical-GPU/provider checks, and hosted release verification. The execution environment could not resolve github.com for local cloning. No foundation completion, rendering-quality certification, or release is claimed.

Next acceptance: use existing server-free browser projects where applicable, then a permitted authoring/save/reload/presentation journey in a non-Production environment; keep real-provider and physical-GPU evidence distinct from fixtures. Preserve current release milestones rather than checking them off because documentation changed.
